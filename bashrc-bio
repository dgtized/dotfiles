#export PATH=${HOME}/pcr_pipeline/expr_analysis:${HOME}/pcr_pipeline/primer_design:${HOME}/pcr_pipeline/lib:${PATH}
export PATH=${HOME}/pcr_pipeline/bin:${PATH}

#export SGE=/bio/tmp/sge_outputs/

export MGC=/bio/exp/mgc
export BIOEXP=/bio/exp/mgc/cc1
export CVSROOT=/bio/home/cvs
export RUBYLIB=$HOME/usr/lib/ruby/site_ruby/1.8
export GEM_HOME=$HOME/usr/lib/ruby/gems
#export GMAP="$HOME/usr/bin/gmap -D/bio/db/gmap -dhg17"

alias qwatch="xterm -geometry 120x80 -e watch 'qstat -u $USER && echo && qstat -g c && echo && qstat -s p' &"
